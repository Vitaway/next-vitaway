import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['cdn.rareblocks.xyz', 'res.cloudinary.com', 'cdn-icons-png.flaticon.com', 'plus.unsplash.com', 'images.unsplash.com', 'ai-tool.nextjstemplates.com', 'vitaway.org', 'upload.wikimedia.org', 'play.google.com'],
  },
};

export default nextConfig;
